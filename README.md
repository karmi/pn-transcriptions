# Transcriptions for _Paměť národa_

Transcribe Czech `.mp3` files stored in Cloudflare R2 using [AssemblyAI](https://www.assemblyai.com/) speech-to-text.

## Install

```bash
uv venv
source .venv/bin/activate
uv pip install assemblyai boto3 botocore tqdm click
```
